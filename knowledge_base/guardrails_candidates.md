# Guardrail Candidates

Lessons awaiting human ratification as hard rules in `knowledge_base/guardrails.yaml`.
Promotion requires explicit human sign-off — never auto-promoted by the Orchestrator.

To promote: append the entry to `guardrails.yaml` with `ratified_by` and `ratified_on` filled in, then remove it from here. See the lessons skill for the exact format.

---

## Pending ratification

*(empty — candidates appear here as the lessons pipeline runs)*
